# Flash-MaxSim：与你的比赛有关的部分

## 1. 要计算什么

给定查询矩阵 \(Q\) 和文档矩阵 \(D\)，先计算每个查询行与每个文档行的点积。对每个查询行，找出其中最大的点积；最后把这些最大值相加：

\[
\text{分数}=\sum_i\max_j\langle Q_i,D_j\rangle
\]

在你的赛题里，这对应：

\[
y[b]=\sum_m\max_n\left(\sum_k x1[b,m,k]\times x2[b,k,n]\right)
\]

也就是：**批量矩阵乘 → 沿 \(N\) 取最大值 → 沿 \(M\) 求和**。每个 batch 只输出一个数。

## 2. 普通实现的问题

普通做法先算出完整的矩阵乘法结果，形状是 `[B, M, N]`，把它存进设备内存；然后读取它，沿 \(N\) 取最大值，再沿 \(M\) 求和。

问题是：这张大矩阵只是临时结果。最终每个 batch 只需要一个数，却要先写入、再读取大量中间数据。当 \(M\) 和 \(N\) 很大时，它会占用大量内存，也可能拖慢计算。

## 3. 论文的核心办法：分块计算，随算随取最大值

不用一次生成完整的 `[B, M, N]` 矩阵。改为从输入中取一小块 \(M\) 行和一小块 \(N\) 列，计算一个小矩阵。

对这小矩阵的每一行，立即取最大值，并更新该行“到目前为止的最大值”。然后计算下一个 \(N\) 列块，继续更新。**所有 \(N\) 列都检查完后**，才把各行的最大值相加。

论文称这种做法为“在线最大值”：这里的“在线”只表示边计算边更新，无须预先保存全部结果。

## 4. 算法 1，改写成适合本题阅读的步骤

对每个 batch：

1. 把最终分数设为 `0`。
2. 取一小组 \(M\) 行。
3. 把这组中**每一行**目前的最大值设为负无穷。
4. 依次取各个 \(N\) 列块：
   - 计算当前行块与列块的矩阵乘法；
   - 对结果的每一行取最大值；
   - 用它更新该行目前的最大值。
5. 扫完全部 \(N\) 列块后，把这组行的最大值加到最终分数。
6. 处理下一组 \(M\) 行，直到全部处理完。
7. 写出这个 batch 的分数。

用一句伪代码表示核心循环：

```text
分数 = 0
对每一组 M 行:
    每行最大值 = 负无穷
    对每一组 N 列:
        小块结果 = 矩阵乘(当前 M 行, 当前 N 列)
        每行最大值 = max(每行最大值, 小块结果的每行最大值)
    分数 += 这些行的最大值之和
```

## 5. 为什么结果仍然正确

把一行的 \(N\) 个数分成若干块，**先取每块的最大值，再从这些最大值中取最大值**，与直接从全部 \(N\) 个数中取最大值相同。因此，分块没有改变题目要求的计算顺序：仍然是先沿 \(N\) 取最大值，再沿 \(M\) 求和。

实际计算使用浮点数，分块可能改变少量加法的先后顺序，所以结果不一定逐位相同，仍须通过赛题的精度检查。

## 6. 用到你的 Ascend C 比赛时，特别注意

论文的实现面向 GPU：它让小块结果留在片上，取完最大值后再覆盖，不把完整相似度矩阵写入全局内存。你的目标是昇腾 NPU，需要另外解决 Cube 矩阵乘结果怎样交给 Vector 做归约；**照搬论文的分块大小或 GPU 性能数字没有意义**。

还必须处理四件事：

- **全负数：**最大值初始值用负无穷，不能用 `0`。
- **尾块：**最后一块不足整块时，无效列不能参与取最大值。
- **存储布局：**赛题的两个转置标志描述输入怎样存放，不能漏掉任何组合。
- **精度：**按赛题要求处理矩阵乘累加、最大值与最终求和，并以平台结果核对。

你现阶段真正要从论文带走的，就是这个计算顺序：**算一小块 → 更新每行最大值 → 扫完所有列块 → 对行最大值求和**。
